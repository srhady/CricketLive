name: CricketLive Poster Studio

on:
  schedule:
    - cron: '*/15 * * * *' # প্রতি ১৫ মিনিট পরপর
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Poster Maker Script
      run: python scripts/poster_maker.py # <--- ফোল্ডারের ভেতরের পাথ

    - name: Commit and Push Changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add posters/ # <--- শুধু posters ফোল্ডারটা সেভ করবে
        git diff --quiet && git diff --staged --quiet || git commit -m "🎨 Auto-updated match posters"
        git push
